# List Menggunakan []
data_list = [1, 2, 3, 4, 5]
print(data_list)

# Tuples menggunakan ()
# Data tuples tidak bisa diubah, ditambah tapi bisa di indexing
data_tuples = (6, 7, 8, 9, 10)
print(data_tuples)
print(data_tuples[1])

# Sets (Himpunan)
# Data sets bisa diubah, ditambah, namun tidak memiliki index
# Besifat sama seperti himpunan
data_sets = {11, 12, 13, 14, 15}
print(data_sets)